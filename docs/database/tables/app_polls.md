# app_polls 表

## 列信息

| 列名 | 数据类型 | 示例值 |
|------|----------|--------|
| （表为空，无法推断结构） | - | "-" |

## 示例查询

```sql
-- 查询所有数据
SELECT * FROM ${tableName} LIMIT 10;

-- 查询数据条数
SELECT COUNT(*) FROM ${tableName};
```
