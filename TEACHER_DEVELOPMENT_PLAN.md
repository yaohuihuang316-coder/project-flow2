# 教师端开发计划

> 基于实际开发进度和 MCP 验证结果

---

## 📋 项目概览

**技术栈:** React + TypeScript + Vite + Supabase  
**开发流程:** 编码 → 本地 Commit → 阶段完成 → 统一部署验证

---

## ✅ 已完成阶段

### Phase 1: 签到码功能 [⏸️ 代码完成]

**MCP 验证状态:** 代码已推送，生产环境功能未显示（部署问题）

#### 已实现功能
- [x] 教师端生成 6 位数字签到码
- [x] 签到码存储在 `whiteboard_data` JSONB 字段
- [x] 5 分钟过期机制
- [x] 学生端签到输入界面（Classroom.tsx）
- [x] 签到码验证和考勤记录创建

#### 代码位置
```
pages/teacher/Classroom.tsx (lines 975-1010)
```

#### 待验证（部署后）
- [ ] 教师点击"生成签到码"显示 6 位数字
- [ ] 学生输入签到码成功签到
- [ ] 教师端实时显示签到学生列表

---

### Phase 2: UI 自适应修复 [✅ MCP 已验证]

**MCP 验证状态:** 桌面端侧边栏正常显示，响应式布局生效

#### 验证截图
- `verify-phase1-02-teacher-classroom.png` - 桌面端布局验证

#### 已实现功能
- [x] **TeacherDashboard.tsx:**
  - 桌面端侧边栏导航（`lg:block`）
  - 移动端底部导航保留
  - 首页两列网格布局（桌面端）
  - 课程页面响应式网格（1/2/3 列）
  - 作业页面两列网格（桌面端）
  - 个人资料页面两列布局

- [x] **Classroom.tsx:**
  - 主容器响应式适配（`max-w-7xl mx-auto`）
  - 桌面端侧边栏 + 移动端底部导航

#### 响应式断点
```css
/* 移动端优先 */
max-w-lg mx-auto          /* 默认移动端约束 */
lg:max-w-none             /* ≥1024px 桌面端全宽 */
lg:grid-cols-2            /* ≥1024px 两列布局 */
lg:grid-cols-3            /* ≥1024px 三列布局 */
lg:hidden                 /* ≥1024px 隐藏移动端元素 */
lg:block                  /* ≥1024px 显示桌面端元素 */
```

---

### Phase 3: 后台教师管理 [✅ 代码完成]

**MCP 验证状态:** 代码已推送，功能已实现

#### 已实现功能（AdminTeachers.tsx）
- [x] 教师列表显示（连接 Supabase 真实数据）
- [x] 教师状态管理（启用/禁用切换）
- [x] 统计数据展示：
  - 课程数统计
  - 学生数统计
  - 评分显示
  - 状态统计卡片
- [x] 搜索和筛选功能（按姓名、邮箱、状态）
- [x] 教师详情弹窗
- [x] 排序功能（最近加入/姓名/学生数）

#### 数据模型
```typescript
interface Teacher {
  id: string;
  name: string;
  email: string;
  avatar?: string;
  phone?: string;
  status: 'active' | 'inactive' | 'pending';
  created_at: string;
  title?: string;
  total_courses: number;
  total_students: number;
  rating: number;
}
```

---

### Phase 4: 公告管理 [✅ 已有实现]

**MCP 验证状态:** AdminAnnouncements.tsx 已有完整实现

#### 已实现功能
- [x] 连接 `app_announcements` 表
- [x] 完整 CRUD 功能
- [x] 定向推送（全部/Free/Pro/Pro+）
- [x] 优先级设置（1-100）
- [x] 有效期设置（开始/结束时间）
- [x] 公告类型（info/success/warning/error）
- [x] 状态管理（启用/禁用）

#### 待完善（前端展示）
- [ ] 学生端顶部公告栏
- [ ] 教师端公告通知
- [ ] 消息中心集成

---

## 🔧 待解决问题

### 高优先级
| 问题 | 影响 | 解决方案 |
|------|------|----------|
| Vercel Token 失效 | 无法手动部署 | 获取新 Token 或使用 Dashboard 部署 |
| 签到功能未验证 | Phase 1 无法确认 | 部署后运行 `verify-phase1.mjs` |

### 中优先级
| 问题 | 影响 | 解决方案 |
|------|------|----------|
| 代码分割警告 | 构建体积大 | 配置 `manualChunks` 优化 |

---

## 📂 关键文件位置

### 教师端页面
```
pages/TeacherDashboard.tsx       # 教师首页（响应式布局）
pages/teacher/Classroom.tsx      # 上课页面（签到码功能）
pages/teacher/MyCourses.tsx      # 我的课程
pages/teacher/Assignments.tsx    # 作业管理
pages/teacher/Profile.tsx        # 个人资料
```

### 后台管理
```
pages/admin/AdminTeachers.tsx    # 教师管理（Phase 3）
pages/admin/AdminAnnouncements.tsx # 公告管理（Phase 4）
pages/admin/AdminLayout.tsx      # 后台布局
```

### 验证脚本
```
verify-phase1.mjs                # 签到功能验证脚本
```

---

## 🚀 部署验证计划

### 步骤 1: 解决部署问题
```bash
# 方案 A: 使用新的 Vercel Token
npx vercel deploy --token=NEW_TOKEN

# 方案 B: Dashboard 手动部署
# 1. 访问 https://vercel.com/dashboard
# 2. 找到 project-flow2
# 3. 点击 Deploy
```

### 步骤 2: 运行验证
```bash
# Phase 1 验证
node verify-phase1.mjs

# 预期结果:
# ✅ 教师生成签到码成功
# ✅ 学生输入签到码成功
# ✅ 教师端实时显示签到列表
```

### 步骤 3: 响应式验证
```bash
# 使用 Playwright 截图验证
# - 375px (iPhone SE)
# - 768px (iPad)
# - 1440px (Desktop)
```

---

## 📊 当前代码状态

### 最新 Commit
```
324f7ff docs: update implementation plan - Phase 2,3,4 progress
```

### 构建状态
```bash
npm run build
# ✅ 构建成功
# dist/index.html
# dist/assets/index-y9JmZwa7.js (1,597.56 kB)
```

### GitHub 状态
```
main 分支已推送至: https://github.com/yaohuihuang316-coder/project-flow2
```

---

## 📝 下一步行动

### 立即行动
1. **解决 Vercel 部署问题**
   - 获取新的 Vercel Token
   - 或手动在 Dashboard 部署

2. **验证 Phase 1 签到功能**
   - 运行 `node verify-phase1.mjs`
   - 确认教师端和学生端交互正常

### 后续开发（可选）
3. **前端公告展示**
   - 学生端顶部公告栏组件
   - 教师端通知提醒

4. **课堂管理增强**
   - AdminClassSessions.tsx（课堂监控）
   - 强制结束课堂功能

5. **作业系统优化**
   - 作业批改界面
   - 成绩统计报表

---

## 🎯 里程碑检查点

| 里程碑 | 状态 | 验证方式 |
|--------|------|----------|
| 响应式布局 | ✅ | MCP 截图验证 |
| 教师管理 | ✅ | 代码审查 |
| 公告管理 | ✅ | 代码审查 |
| 签到功能 | ⏳ | 待部署验证 |

---

*文档生成时间: 2026-02-17*  
*最后更新: 324f7ff*
